// STUB — interface contract only. Task B2 replaces this file wholesale.
// Props are fixed here so B1 (App routing) and B2 (the flow) can be built in
// parallel: `first` = no attempts exist yet, so there is nowhere to go back to;
// `onExit` returns to the Front door without saving anything.
export default function SetupFlow({ first = false, onExit }) { // eslint-disable-line no-unused-vars
  return null;
}
