// The craving triggers the app offers as chips. Data, not UI: the SOS screen,
// the log toast, stats and the api's reason guard all read this one list.
export const TRIGGERS = ['after-meal', 'coffee', 'driving', 'stress', 'boredom', 'social'];
